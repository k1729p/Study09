spring:
  jackson:
    deserialization:
      FAIL_ON_NULL_FOR_PRIMITIVES: false
  output:
    ansi:
      enabled: always
# enable all Actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: "*"